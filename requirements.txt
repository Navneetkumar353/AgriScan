# Streamlit UI
streamlit>=1.33

# Force headless OpenCV (no libGL needed) — keep this ABOVE ultralytics
opencv-python-headless==4.10.0.84

# YOLO
ultralytics>=8.3.0

# PyTorch CPU wheels compatible with Python 3.12
torch==2.5.1
torchvision==0.20.1

# Utils
pillow
numpy
langchain>=0.2
langchain-groq>=0.1.5
python-dotenv>=1.0
